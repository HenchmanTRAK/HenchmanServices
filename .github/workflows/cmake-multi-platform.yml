# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: CMake on multiple platforms

on:
  push:
    branches: 
      - main
    tags:
      - "v*.*.*"

env:
  VERSION: "1.1.5"
  CURR_VERSION: "1.1.5"

jobs:
  setup:
    runs-on: ${{ matrix.os }}
    
    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: true

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [windows-latest]
        build_type: [Release]
        c_compiler: [cl]
        arch: [x64]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
            arch: x64
    outputs:
        VERSION: ${{steps.set-version.outputs.version}}
        CURR_VERSION: ${{steps.find-latest-tag.outputs.tag}}
    steps:
    - uses: actions/checkout@v4

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: "Checking for OpenSSL"
      shell: bash
      run: |
        ls -alS "C:/Program Files/OpenSSL"

    - name: Set version variables
      id: set-version
      shell: bash
      run: |
        VER=$(cat version)
        echo "VERSION=$VER" >> $GITHUB_ENV
        echo "version=$VER" >> $GITHUB_OUTPUT

    - name: "Getting current release version"
      uses: oprypin/find-latest-tag@v1
      with:
        repository: ${{ github.repository }}
        releases-only: true
        regex: '^\d+\.\d+\.\d+$'
      id: find-latest-tag

    - name: "Setting current release version"
      shell: bash
      run: echo "CURR_VERSION=${{ steps.find-latest-tag.outputs.tag }}" >> $GITHUB_ENV

  
  install_openssl:
    needs: [setup]
    if: ${{ needs.setup.outputs.VERSION > needs.setup.outputs.CURR_VERSION }}
    runs-on: ${{ matrix.os }}
    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: true

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [windows-latest]
        build_type: [Release]
        c_compiler: [cl]
        arch: [x64]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
            arch: x64
    steps:
    - uses: actions/checkout@v4
    
    - name: "Checking needs output"
      shell: bash
      run: |
        echo ${{needs.setup.outputs.VERSION}}
        echo ${{needs.setup.outputs.CURR_VERSION}}

    - run: mkdir -p C:/vcpkg/packages
  
    - name: Cache OpenSSL results
      uses: actions/cache@v3
      id: cache-vcpkg-packages
      env:
          cache-name: cache-vcpkg-packages
      with: 
        path: "C:/vcpkg/packages"
        key: ${{ matrix.os }}-build-${{ env.cache-name }}
        restore-keys: |
          ${{ matrix.os }}-build-${{ env.cache-name }}
          ${{ matrix.os }}-build
          ${{ matrix.os }}-

    - if: ${{ steps.cache-vcpkg-packages.outputs.cache-hit != 'true' }}
      name: Install OpenSSL (Windows)
      id: "openssl"
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl

    - uses: actions/upload-artifact@master
      with:
        name: openssl_windows
        path: C:/vcpkg/packages
    
  install_doxy:  
    needs: [setup]
    if: ${{ needs.setup.outputs.VERSION > needs.setup.outputs.CURR_VERSION }}
    runs-on: ${{ matrix.os }}
    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: true

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [windows-latest]
        build_type: [Release]
        c_compiler: [cl]
        arch: [x64]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
            arch: x64
    steps:
    - uses: actions/checkout@v4
  
    - run: mkdir -p "C:/Program Files/Graphviz"
  
    - name: Cache Graphviz results
      uses: actions/cache@v3
      id: cache-Graphviz
      env:
          cache-name: cache-Graphviz
      with: 
        path: "C:/Program Files/Graphviz"
        key: ${{ matrix.os }}-build-${{ env.cache-name }}
        restore-keys: |
          ${{ matrix.os }}-build-${{ env.cache-name }}
          ${{ matrix.os }}-build
          ${{ matrix.os }}-

    - if: ${{ steps.cache-Graphviz.outputs.cache-hit != 'true' }}
      name: Setup Graphviz
      uses: ts-graphviz/setup-graphviz@v2

    - uses: actions/upload-artifact@master
      with:
        name: Doxy_Graphviz
        path: "C:/Program Files/Graphviz"

  build:
    needs: [setup, install_openssl, install_doxy]  
    if: ${{ needs.setup.outputs.VERSION > needs.setup.outputs.CURR_VERSION }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
      pull-requests: write
      repository-projects: write

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: true

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [windows-latest]
        build_type: [Release]
        c_compiler: [cl]
        arch: [x64]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
            arch: x64

    steps:
    - uses: actions/checkout@v4
  
    - name: Generate empty ini file.
      id: ini_file
      shell: cmd
      run: |
        cd ${{ github.workspace }} && echo .>service.ini

    - uses: ilammy/msvc-dev-cmd@v1.4.1
      with:
        arch: ${{ matrix.arch }}
    
    - uses: ssciwr/doxygen-install@v1
  
    - uses: actions/download-artifact@master
      with:
        name: Doxy_Graphviz
        path: "C:/Program Files/Graphviz"
    
    - uses: actions/download-artifact@master
      with:
        name: openssl_windows
        path: C:/vcpkg/packages

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: '6.7.2'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_msvc2019_64'
        dir: '${{ github.workspace }}/'
        install-deps: 'true'
        cache: 'true'

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ github.workspace }}/build
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -S ${{ github.workspace }}

    - name: "Checking for version set"
      run: echo VERSION=$env:VERSION

    - name: Build
      id: cmake_build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ github.workspace }}/build --config ${{ matrix.build_type }} --target PACKAGE && echo "output-exe=${*.exe}" >> "$env:GITHUB_OUTPUT"

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest --build-config ${{ matrix.build_type }}

    # - uses: actions-ecosystem/action-get-merged-pull-request@v1
    #   id: get-merged-pull-request
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}

    # - uses: actions-ecosystem/action-release-label@v1
    #   id: release-label
    #   if: ${{ steps.get-merged-pull-request.outputs.title != null }}
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}
    #     labels: ${{ steps.get-merged-pull-request.outputs.labels }}

    # - uses: actions-ecosystem/action-get-latest-tag@v1
    #   id: get-latest-tag
    #   if: ${{ steps.release-label.outputs.level != null }}
    #   with:
    #     semver_only: true

    # - uses: actions-ecosystem/action-bump-semver@v1
    #   id: bump-semver
    #   if: ${{ steps.release-label.outputs.level != null }}
    #   with:
    #     current_version: ${{ steps.get-latest-tag.outputs.tag }}
    #     level: ${{ steps.release-label.outputs.level }}
    #   with:
    #     current_version: $VERSION
    #     level: minor

    # - uses: actions-ecosystem/action-regex-match@v2
    #   id: regex-match
    #   if: ${{ steps.bump-semver.outputs.new_version != null }}
    #   with:
    #     text: ${{ steps.get-merged-pull-request.outputs.body }}
    #     regex: '```release_note([\s\S]*)```'

    # - uses: actions-ecosystem/action-push-tag@v1
    #   if: ${{ steps.bump-semver.outputs.new_version != null }}
    #   with:
    #     tag: ${{ steps.bump-semver.outputs.new_version }}
    #     message: "${{ steps.bump-semver.outputs.new_version }}: PR ${{ github.event.pull_request.number  }} ${{ github.event.pull_request.title }}"

    # - uses: actions/create-release@v1
    #   id: create_release
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     tag_name: ${{env.VERSION}}
    #     release_name: Release ${{env.VERSION}}

    # - name: Upload Release
    #   id: upload-release
    #   uses: actions/upload-release-asset@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.create_release.outputs.upload_url }}
    #     asset_path: "${{ github.workspace }}/build/HenchmanServices-${{env.VERSION}}-win64.exe"
    #     asset_name: "HenchmanServices-${{env.VERSION}}-win64.exe"
    #     asset_content_type: application/x-msdownload

    - name: Release
      uses: softprops/action-gh-release@v2
      #if: startsWith(github.ref, 'refs/tags/')
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{env.VERSION}}
        name: Release ${{env.VERSION}}
        files: "${{ github.workspace }}/build/HenchmanServices-${{env.VERSION}}-win64.exe"

    # - uses: actions-ecosystem/action-create-comment@v1
    #   if: ${{ steps.bump-semver.outputs.new_version != null }}
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}
    #     number: ${{ github.event.pull_request.number }}
    #     body: |
    #       The new version [${{ steps.bump-semver.outputs.new_version }}](https://github.com/${{ github.repository }}/releases/tag/${{ steps.bump-semver.outputs.new_version }}) has been released :tada:
